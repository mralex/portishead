<% title @page.title %>
<% if current_user %>
	<% content_for :nav do %>
		| <strong><%= link_to 'Edit Page', edit_page_path(@page) %></strong>
	<% end %>
<% end %>

<h1 class="grid_12"><%= @page.title %></h1>

<div id="page">
  <%= @page.content.html_safe %>
  <div class="clear"></div>
</div>
