<%= semantic_form_for(@project, :html => {:multipart => true}) do |f| %>
	<%= f.inputs :name, :description, :category, :worked_on %>
	<%= f.inputs :for => :images do |i| %>
		<%= render "image_attachment", :i => i%>
	<% end %>
	<%= f.buttons %>
<% end %>
