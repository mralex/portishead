<%= semantic_form_for(@client) do |f| %>
	<%= f.inputs do %>
		<%= f.input :name %>
		<%= f.input :description, :as => :text %>
		<%= f.input :url %>
		<%= f.input :started_at %>
		<%= f.input :ended_at %>
	<% end %>
	<%= f.buttons %>
<% end %>
